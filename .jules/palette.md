# Critical UX and Accessibility Learnings

* Wrapped IconButtons that provide essential interaction without visible text labels (such as theme toggle and more options menu) in `Semantics` widgets with `label` and `button: true`, and added `tooltip`s. This ensures these controls are accessible to screen readers and have proper visual cues for other users.
