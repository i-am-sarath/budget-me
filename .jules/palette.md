# Palette Journal
- Ensure `IconButton` widgets include a `tooltip` property and consider wrapping them in a `Semantics` widget with a descriptive `label` and `button: true` for complete and proper accessibility semantics.
