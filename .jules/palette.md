## Dashboard Actions Accessibility
Added `Semantics` widget with `label` and `button: true` along with `tooltip` property to the `IconButton` widgets in the Dashboard screen's app bar actions (theme toggle, more options) to improve screen reader and pointer accessibility.
