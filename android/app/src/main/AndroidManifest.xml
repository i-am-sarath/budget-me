<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- The app has no gallery / photo-picker feature, so it must NOT request
         persistent media access. Google Play rejects READ_MEDIA_IMAGES /
         READ_MEDIA_VIDEO for apps that only need one-time/infrequent access.
         tools:node="remove" strips them even if a dependency injects them via
         manifest merge. Legacy external-storage perms are removed for the same
         reason — nothing in the app reads or writes shared storage. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <!-- Always-on quick-log overlay (flutter_overlay_window). Android only.
         SYSTEM_ALERT_WINDOW is heavily restricted by Play Store; user must
         manually grant via system settings on Android 11+. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- The always-on quick-log overlay (flutter_overlay_window) runs as a
         foreground service. These permissions are REQUIRED — without them the
         overlay service cannot start (SecurityException on Android 9+) and the
         bubble never appears. They do require a Foreground Service declaration
         in the Play Console listing at submission time. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>

    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <application
        android:label="Money Purse"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config">
        <!-- allowBackup=false: this is a finance app. The local SQLite DB holds
             every transaction and SharedPreferences holds the user's email and
             linked-sheet id, so we opt out of Android auto-backup / adb backup
             rather than let that data leave the device unencrypted. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-6384260983473792~8804057473"/>

        <!-- Home-screen quick-add widget. Tapping it launches MainActivity with
             the budgetme://voice deep link (handled in main.dart) which opens
             voice recording. -->
        <receiver
            android:name=".BudgetWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/budget_widget_info" />
        </receiver>
        <!-- flutter_overlay_window's OverlayService is NOT declared in the
             plugin's own manifest — the app must declare it, or startService()
             throws and the overlay bubble never appears. It runs as a
             "specialUse" foreground service; the <property> is required on
             Android 14 (API 34+) to declare why. -->
        <service
            android:name="flutter.overlay.window.flutter_overlay_window.OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:node="merge">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Always-on quick-log voice recording bubble"/>
        </service>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
